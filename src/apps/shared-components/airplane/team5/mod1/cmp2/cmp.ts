
import {Component} from '@angular/core';
@Component({
  selector: 'cmp-512',
  template: '<strong>512</strong>&nbsp;',
})
export class Cmp512Component {
  add512(x: number) {
    return x + 512;
  }
}