console.log(__dirname);

// import * as express from './cdn.runfiles/angular_samples/node_modules/express';

// const app = express();

// const base = `${__dirname}/bundler-comparison-app.runfiles/angular_samples`;
// const root = `${base}/src/apps/bundler-comparison-app`;

// app.use(express.static('/'), express.static(root));

// app.listen(4001, () => console.log('Example app listening on port 4001!'));
